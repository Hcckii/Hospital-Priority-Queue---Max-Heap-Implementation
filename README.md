# Hospital-Priority-Queue---Max-Heap-Implementation
A hospital emergency room patient management system using max heap priority queue in C.  Features: - Add patients with priority levels - Automatically serve highest priority patient first - View all waiting patients - Efficient O(log n) operations 
